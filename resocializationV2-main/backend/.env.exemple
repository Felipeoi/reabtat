# Copie para backend/.env (dev local) ou use como base para backend/dist/.env (Docker: host db, não 127.0.0.1).

APP_ENV=dev
PORT=8080

# PostgreSQL — banco deste projeto: resocialization_v2 (schema SQL continua "resocialization")
DB_DSN=postgres://postgres:1212@127.0.0.1:5433/resocialization_v2?sslmode=disable

JWT_SECRET=super-secret-mude-isto

# CORS / links (opcional)
FRONTEND_URL=http://localhost:5173
